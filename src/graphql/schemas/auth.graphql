type AuthResponse {
    token: String!
    user: User!
}

input SignInInput {
    email: String!
    password: String!
}

input CreateUserInput {
    name: String!
    email: String!
    password: String!
}
